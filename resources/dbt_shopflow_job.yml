resources:
  jobs:
    dbt_databricks_job:
      name: dbt_databricks_job
      tasks:
        - task_key: First_task
          dbt_task:
            project_directory: ${workspace.root_path}/files/
            commands:
              - dbt deps
              - dbt seed
              - dbt snapshot
              - dbt run
              - dbt test
            schema: bronze
            warehouse_id: ${var.warehouse_id}
            catalog: ${var.catalog}
            source: WORKSPACE
          environment_key: dbt-default
      queue:
        enabled: true
      environments:
        - environment_key: dbt-default
          spec:
            dependencies:
              - dbt-databricks>=1.0.0,<2.0.0
            environment_version: "5"
      performance_target: PERFORMANCE_OPTIMIZED
